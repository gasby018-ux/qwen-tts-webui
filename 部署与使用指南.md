# Qwen TTS WebUI 部署与使用指南(本机实测版)

> 适用环境:autodl 服务器(单卡 RTX 5090 32G),已按本文档完整跑通。
> 本文档命令均为本机实测路径,可直接复制执行。

---

## 0. 环境总览

| 项 | 值 |
|---|---|
| 项目目录 | `/root/autodl-tmp/chg/avatar/qwen-tts-webui` |
| Python 环境 | 复用 JoyAI venv:`/root/autodl-tmp/chg/avatar/JoyAI-VL-Interaction/services/.venv`(torch 2.11.0+cu130 已就位) |
| 模型存放 | `/root/autodl-tmp/models/`(数据盘) |
| HF 缓存 | `/root/autodl-tmp/hf-cache`(数据盘,避免系统盘 3.3G 被撑爆) |
| 服务端口 | 6006(WebUI + API 一体) |

**⚠️ 磁盘提醒**:数据盘 `/root/autodl-tmp` 已 90%+ 被占用,下载前先 `df -h /root/autodl-tmp` 确认剩余空间;模型下载不走 HF 默认缓存(系统盘),全部指到数据盘。

---

## 1. 安装依赖

依赖 torch(已有)+ qwen-tts + gradio 等,装进 JoyAI venv:

```bash
cd /root/autodl-tmp/chg/avatar/qwen-tts-webui
uv pip install --python /root/autodl-tmp/chg/avatar/JoyAI-VL-Interaction/services/.venv/bin/python -r requirements.txt
```

> 注意:requirements.txt 中的 torch/torchvision/torchaudio 已满足(venv 里有 cu130 版),不会重复下载;`qwen-tts` 会把 `transformers` 降到 4.57.3(与 JoyAI 主栈的 5.15.1 不同,共存时注意)。

---

## 2. 下载模型

### 2.1 声音生成模型(CustomVoice,含 9 个内置 speaker)

```bash
export HF_ENDPOINT=https://hf-mirror.com
export HF_HOME=/root/autodl-tmp/hf-cache
export HF_HUB_LOCAL_DIR_AUTO_SYMLINK_THRESHOLD=0

hf download Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice \
  --local-dir /root/autodl-tmp/models/Qwen3-TTS-12Hz-1.7B-CustomVoice
```

### 2.2 声音克隆模型(Base,克隆功能必需)

```bash
hf download Qwen/Qwen3-TTS-12Hz-1.7B-Base \
  --local-dir /root/autodl-tmp/models/Qwen3-TTS-12Hz-1.7B-Base
```

### 2.3 构造 HF 缓存(hardlink,免重复下载、零额外磁盘)

`hf download --local-dir` 只写 refs 元数据、不建 snapshots,直接运行会重新下载 3.6G。
每个模型下载完后执行一次(把 `$REPO` 换成模型名):

```bash
REPO=Qwen3-TTS-12Hz-1.7B-CustomVoice   # 或 Qwen3-TTS-12Hz-1.7B-Base
SHA=$(cat /root/autodl-tmp/hf-cache/hub/models--Qwen--${REPO}/refs/main)
SNAP="/root/autodl-tmp/hf-cache/hub/models--Qwen--${REPO}/snapshots/$SHA"
mkdir -p "$SNAP"
cp -al /root/autodl-tmp/models/$REPO/* "$SNAP/" 2>/dev/null
cp -al /root/autodl-tmp/models/$REPO/.[!.]* "$SNAP/" 2>/dev/null
```

### 2.4 关键:服务端必须走 huggingface 通道(已改默认值)

webui 原始默认 `api_type="modelscope"`,加载模型走 modelscope 下载、缓存落在
**项目内 `cache/modelscope/`**(与上述 HF 通道互不相通,会导致每个模型重复下载一份 3.6-4.7G)。
已修改 `qwen_tts_webui/config_manager/shared.py` 默认值为 `huggingface`,
服务端 `snapshot_download(local_files_only=True)` 会命中 2.3 节构造的本地缓存,零网络下载。
新部署请确认该默认值未被覆盖(设置页「下载模型的 API 类型」= huggingface)。

### 2.5 可选:其他模型(语音设计/0.6B)

| 模型 | 用途 |
|---|---|
| `Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign` | Voice Design 标签页(按描述设计音色) |
| `Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice` | 轻量版声音生成 |
| `Qwen/Qwen3-TTS-12Hz-0.6B-Base` | 轻量版克隆 |

下载方式同上(`hf download <repo> --local-dir ...` + 2.3 节缓存构造)。

---

## 3. 启动服务

```bash
cd /root/autodl-tmp/chg/avatar/qwen-tts-webui
HF_HOME=/root/autodl-tmp/hf-cache HF_ENDPOINT=https://hf-mirror.com \
nohup /root/autodl-tmp/chg/avatar/JoyAI-VL-Interaction/services/.venv/bin/python \
  launch.py --server-name 0.0.0.0 --server-port 6006 --no-inbrowser --skip-check --api \
  > /tmp/ttswebui.log 2>&1 &
```

参数说明:

| 参数 | 作用 |
|---|---|
| `--server-name 0.0.0.0` | 允许外部访问(autodl 端口映射) |
| `--server-port 6006` | 服务端口 |
| `--no-inbrowser` | 服务器无浏览器,禁止自动打开 |
| `--skip-check` | 跳过依赖检查(已装好,加快启动) |
| `--api` | 启用 REST API(同时保留 WebUI) |

停止服务(注意 `[l]` 技巧防自杀):

```bash
pkill -f "[l]aunch.py"
```

**访问地址**:
- WebUI: `http://<autodl地址>:6006`(需在 autodl 控制台映射 6006 端口)
- API 文档: `http://<autodl地址>:6006/docs`
- API 根: `http://<autodl地址>:6006/qwenapi/v1/`

---

## 4. 验证

```bash
# 服务存活
curl -s -o /dev/null -w "WebUI: %{http_code}\n" http://127.0.0.1:6006/

# 冷启动(模型未加载)即可枚举 speaker / 语言 —— 懒加载修复后的行为
curl -s http://127.0.0.1:6006/qwenapi/v1/speakers
curl -s http://127.0.0.1:6006/qwenapi/v1/languages

# 生成一段语音(指定 speaker)
curl -s -X POST http://127.0.0.1:6006/qwenapi/v1/custom-voice \
  -H "Content-Type: application/json" \
  -d '{"model_name":"Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice","text":"你好,这是测试。","speaker":"serena"}' \
  -o /tmp/tts_out.json
# 解析 base64 保存 wav
python3 -c "import json,base64; d=json.load(open('/tmp/tts_out.json')); open('/tmp/test.wav','wb').write(base64.b64decode(d['audio_files_base64'][0])); print('saved /tmp/test.wav')"
```

首次生成会加载模型到 GPU(约 1-6 分钟,含 CUDA 编译),之后常驻(~5.4G 显存),单次生成 3-8 秒。

---

## 5. WebUI 使用

### 5.1 声音生成(Custom Voice)

- 模型: `Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice`
- **发言人**:9 个内置 speaker,页面加载即可选(已修复懒加载,无需先生成一次):

| 参数名 | 音色 |
|---|---|
| `vivian` | 明亮、带点锐利的年轻女声 |
| `serena` | 温暖温柔的年轻女声 |
| `uncle_fu` | 低沉醇厚的中年男声 |
| `dylan` | 北京口音青年男声 |
| `eric` | 成都口音男声 |
| `ryan` | 节奏感强的英文男声 |
| `aiden` | 阳光美式男声 |
| `ono_anna` | 轻快的日语女声 |
| `sohee` | 情感丰富的韩语女声 |

- **语言**:`auto` 会由模型自主检测文本语言;选 `dylan`/`eric` 时语言自动带出北京/四川口音(方言联动)
- `声音特征描述`(instruct):可选,如"用温柔的语气说",可叠加在 speaker 之上

### 5.2 声音克隆(Voice Clone)

- 模型: `Qwen/Qwen3-TTS-12Hz-1.7B-Base`
- 参考音频:5-15 秒、干净无噪音的单一人声,效果好于合成音频
- `ref_text` 一般留空效果更好
- **两阶段工作流**(同页完成,不必下载再上传):
  1. 第一阶段:上传参考音频、输入文本,点`开始生成`
  2. 第二阶段(可选):设置开头/末尾静音秒数(默认末尾 3 秒),点`给生成音频加静音`。可改时长后重复第二阶段,始终基于原始克隆结果

### 5.3 静音拼接(ffmpeg)

与 TTS 共用同一端口,打开 WebUI 顶部「静音拼接」标签即可,无需再单独起 `audio-silence-web`。

- 生成指定时长静音音频
- 上传两段音频,中间插入指定时长静音后拼接
- 上传任意音频,在开头或末尾补指定时长静音
- 支持上传、预览、下载;结果保存在 `outputs/silence/`,也可在「音频浏览」中查看

### 5.4 卸载模型释放显存

克隆/生成一次后,Qwen TTS 模型会留在 GPU 里加快下次推理。若要接着跑 FlashTalk 等数字人任务,先点页面顶部 **「卸载模型并释放显存」**,或:

```bash
curl -s -X POST http://127.0.0.1:6006/qwenapi/v1/unload
```

下次再点「开始生成」会自动重新加载模型。

---

## 6. API 速查

```bash
BASE=http://127.0.0.1:6006/qwenapi/v1

# 生成(指定 speaker;不传 speaker 默认 aiden)
POST $BASE/custom-voice
{"model_name":"Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice","text":"...","speaker":"dylan","instruct":"..."}

# 克隆(需要 Base 模型)
POST $BASE/voice-clone
{"model_name":"Qwen/Qwen3-TTS-12Hz-1.7B-Base","text":"...","ref_audio_base64":"<base64>","ref_text":"(可选)"}

# 中断当前任务
POST $BASE/interrupt

# 查询 TTS 是否占着显存
GET $BASE/memory

# 卸载 TTS 模型并回收显存 (去跑 FlashTalk 前先调这个)
POST $BASE/unload
```

**⚠️ 注意**:`language` 参数用 `"chinese"` 等全名,`"zh"` 会报 `Unsupported languages`。

---

## 7. 踩坑记录

1. **pkill 自杀**:`pkill -f "launch.py"` 会匹配到命令行含该串的 bash 自身 → 用 `pkill -f "[l]aunch.py"`(自排除技巧)
2. **模型重复下载**:`hf download --local-dir` 后必须按 2.3 节构造 snapshots 硬链接,否则 from_pretrained 会重下 3.6G
3. **语言参数**:`zh` ❌ → `chinese` ✅(qwen_tts 校验)
4. **磁盘**:HF 默认缓存走系统盘(仅剩 ~3G),务必 `HF_HOME=/root/autodl-tmp/hf-cache`
5. **transformers 降级**:装 qwen-tts 会把 venv 的 transformers 5.15.1 → 4.57.3,影响 JoyAI 主栈时注意(可给本服务单独 venv)
6. **首次生成慢**:模型加载 + CUDA 编译 1-6 分钟,非卡死;显存 ~5.4G
7. **modelscope 通道坑**:webui 默认 api_type=modelscope,与 HF 通道缓存互不相通,每个模型会从 modelscope.cn 重复下载一份(实测三模型 13G)。已改默认 huggingface;若设置页被改回 modelscope,会再次出现"已下载还重下"现象

---

## 8. 本次代码修改记录(懒加载修复)

| 文件 | 改动 |
|---|---|
| `qwen_tts_webui/backend/qwen_backend.py` | `get_supported_speakers`/`get_supported_languages` 支持模型未加载时从本地 config.json 枚举;新增 `_load_config_metadata()` |
| `qwen_tts_webui/frontend.py` | 页面加载 + 切模型事件即刷新 speaker/language 下拉;去掉 "default" 选项;下拉默认选中真实 speaker |
| `qwen_tts_webui/api/api.py` | `/speakers` `/languages` 冷启动时用默认模型枚举;`speaker` 缺省仍兜底 speakers[0] |
| `qwen_tts_webui/config_manager/shared.py` | `api_type` 默认 `modelscope` → `huggingface`,让服务端复用 HF 本地缓存,避免 modelscope 通道重复下载 13G |
| `qwen_tts_webui/silence_ui.py` / `ffmpeg_utils.py` | 将静音拼接工具并入 WebUI「静音拼接」标签,与 TTS 共用同一端口 |
